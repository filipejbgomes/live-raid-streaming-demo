apiVersion: apps/v1
kind: Deployment
metadata:
  name: bossraid-player-generator
  namespace: demo
spec:
  replicas: 0
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: bossraid-player-generator
  template:
    metadata:
      labels:
        app: bossraid-player-generator
    spec:
      terminationGracePeriodSeconds: 60
      containers:
      - name: bossraid-player-generator
        image: bossraid-player-generator:local
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            memory: 256Mi
        env:
        - name: PLAYERS
          value: '500'
        - name: RATE
          value: '500'
      imagePullSecrets: []
