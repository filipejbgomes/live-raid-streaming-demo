apiVersion: v1
kind: ServiceAccount
metadata:
  name: bossraid-verifier
  namespace: demo
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: verifier-data
  namespace: demo
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: __STORAGE_CLASS__
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bossraid-verifier
  namespace: demo
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: bossraid-verifier
  template:
    metadata:
      labels:
        app: bossraid-verifier
    spec:
      terminationGracePeriodSeconds: 60
      containers:
      - name: bossraid-verifier
        image: bossraid-verifier:local
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            memory: 1Gi
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /live
            port: 8080
          initialDelaySeconds: 90
          periodSeconds: 10
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: data
          mountPath: /data
      serviceAccountName: bossraid-verifier
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: verifier-data
      imagePullSecrets: []
---
apiVersion: v1
kind: Service
metadata:
  name: bossraid-verifier
  namespace: demo
spec:
  selector:
    app: bossraid-verifier
  ports:
  - port: 8080
    targetPort: 8080
